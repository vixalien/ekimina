services:
  postgres:
    image: postgres:17-alpine
    container_name: ekimina-postgres
    environment:
      POSTGRES_USER: ekimina
      POSTGRES_PASSWORD: ekimina
      POSTGRES_DB: ekimina
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
